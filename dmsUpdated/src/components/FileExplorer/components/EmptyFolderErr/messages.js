import {defineMessages} from 'react-intl';

export default defineMessages({
    emptyFolderErr: {
        id: "app.components.emptyFolderErr.title",
        defaultMessage: "This folder is empty",
    },
});
