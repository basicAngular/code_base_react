import {defineMessages} from 'react-intl';

export default defineMessages({
    searchDocument: {
        id: "app.components.serviceModal.searchDocument",
        defaultMessage: "Connect document",
    }
});
